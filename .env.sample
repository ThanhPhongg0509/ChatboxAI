# Copy this file to .env and fill in real values. Never commit .env.
OPENAI_API_KEY=sk-REPLACE-WITH-YOUR-OWN-KEY
VECTOR_STORE_NAME=optibot-kb
# After the first successful run, main.py will print the created vector
# store id. Paste it here so subsequent runs reuse the same store instead
# of creating a new one each time.
VECTOR_STORE_ID=
